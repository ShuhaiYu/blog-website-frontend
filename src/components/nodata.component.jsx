const NoDataMessage = ({ message }) => {
    return (
        <div className="test-center w-full p-4 rounded-full bg-grey/50 mt-4">
            <p>{message}</p>
        </div>
    )
}

export default NoDataMessage;
